include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true
  errors:
    avoid_print: error
  exclude:
    - build/**
    - macos/**
    - windows/**
    - linux/**
